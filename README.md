# Pomodoro Timer

一个从简单版本开始逐步演进的 Android 番茄钟项目。

## V1 范围

- Kotlin + Android View(XML) 实现
- 固定 25 分钟专注倒计时
- 支持开始、暂停、重置
- 首版先不加入休息周期、通知、历史记录和设置

## 本地构建

在项目根目录执行：

```powershell
.\gradlew.bat assembleDebug
.\gradlew.bat testDebugUnitTest
```

需要本机已安装：

- JDK 17
- Android SDK

